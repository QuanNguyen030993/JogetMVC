using Microsoft.AspNetCore.Mvc;
using SurveyReportRE.Controllers.Base;
using SurveyReportRE.Models.Migration.Business.Data;

[ApiController]
[Route("api/[controller]/[action]")]
public class SummaryController : BaseControllerApi<Summary>
{
    private readonly IBaseRepository<Summary> _BaseRepository;
    private readonly IConfiguration configuration;

    public SummaryController(IBaseRepository<Summary> BaseRepository, IConfiguration config,IHttpContextAccessor httpContextAccessor) : base(BaseRepository,httpContextAccessor)
    {
        configuration = config;
        _BaseRepository = BaseRepository;
    }

}

