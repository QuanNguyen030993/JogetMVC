@page
@model EnvironmentModel
@{

}
<script>
    $(function () {

          $("#tabPanel").dxSelectBox({
            dataSource:["Default"],
             searchEnabled: true,
             width: 300,
             onValueChanged: function (e) { // handle after select
                  $.ajax({
                      url: `/api/Menu/DbContextEnvironmentChange/${e.value}`,
                      success: function (response) {
                      },
                      error: function (err) {
                      }
                  });
             },
        });


        $("#jogetPanel").dxSelectBox({
            dataSource:["UAT", "Joget"],
             searchEnabled: true,
             width: 300,
             onValueChanged: function (e) { // handle after select
                  $.ajax({
                      url: `/api/Menu/DbContextJogetEnvironmentChange/${e.value}`,
                      success: function (response) {
                      },
                      error: function (err) {
                      }
                  });
             },
        });
    });
</script>
<div>TMIV environment
    <div id="tabPanel">
        @* <div id="dataGrid_@UsersModel.ModelName"></div> *@
    </div>
</div>
<div>Joget environment
    <div id="jogetPanel">
    @* <div id="dataGrid_@UsersModel.ModelName"></div> *@
</div>
</div>
