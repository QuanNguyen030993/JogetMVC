@page
@model SLAModel
@{

}
<script>
    $(function () {
        new MGrid(null, $(`#dataGrid_@SLAModel.ModelName`), new SLAGridOption('@SLAModel.ModelName', "User"), null);
    });
</script>
<div class="dExForm sla-list-page">
    <div class="sla-list-heading">
        <div>
            <h2>SLA configuration</h2>
            <p>Choose a department to update its expected response and processing time.</p>
        </div>
        <span class="sla-list-help"><i class="fas fa-info-circle"></i> Values are maintained by business users</span>
    </div>
    <div id="dataGrid_@SLAModel.ModelName"></div>
</div>

<style>
    .sla-list-page {
        display: flex;
        flex-direction: column;
        width: 100%;
        height: 100%;
        min-height: 0;
        padding: 12px;
        overflow: hidden;
        box-sizing: border-box;
    }

    .sla-list-page > .mgrid-host {
        flex: 1 1 auto;
        min-height: 0;
    }

    .sla-list-heading {
        flex: 0 0 auto;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        margin-bottom: 10px;
        padding: 12px 14px;
        border: 1px solid var(--c-e2e8f0);
        border-radius: 11px;
        background: var(--c-f8fafc);
    }

    .sla-list-heading h2 {
        margin: 0 0 4px;
        color: var(--c-172235);
        font-size: 20px;
        font-weight: 750;
    }

    .sla-list-heading p {
        margin: 0;
        color: var(--c-64748b);
        font-size: 13px;
    }

    .sla-list-help {
        flex: 0 0 auto;
        padding: 7px 10px;
        border-radius: 999px;
        background: var(--c-eef2f7);
        color: var(--c-475569);
        font-size: 12px;
        font-weight: 650;
    }

    @@media (max-width: 700px) {
        .sla-list-page { padding: 12px; }
        .sla-list-heading { align-items: flex-start; flex-direction: column; }
    }
</style>
