@page "{loadParams?}"
@model ManagementModel
@{
    ViewData["Title"] = "Management";
}
@await Html.PartialAsync("_LayoutReference")
@await Html.PartialAsync("_FormStageDelay")
@await Html.PartialAsync("_AppUtil")
<script src="~/js/app/appUtil.js"></script>
<link rel="stylesheet" href="~/css/app/user-guide.css" asp-append-version="true" />
<link rel="stylesheet" href="~/lib/tmivcom/style.css" asp-append-version="true" />
@await Html.PartialAsync("_EnumConstant")
@await Html.PartialAsync("_EntityConstant")
<script>

        const managementUserGuideEnabled = true;
    // $.tmivnotify({
    //         title: "Notification",
    //         content: "Heelo" || "",
    //         type: "info",
    //         position: "bottom-right",
    //         duration: 6000
    //     });
        var callElementView = function (url, code, caption, data) {
        //not invoke to the mForm js

        try {
            const sourceData = data;
            if (url.length === 0) {
                appNotify({ message: "Page request does not exist.", type: 'info' });
                return false;
            }
            var routes = url.split('/');
            if (["null", "undefined"].indexOf(routes[routes.length - 1]) > -1) {
                appNotify({ message: "Incorrect web link, please contact Admin.", type: 'info' });
                return false;
            }
            $.ajax({
                url: addURLParams(url, { UITabId: code, Caption: caption, Data: data != undefined ? data : {} }),
                headers: { 'Content-Type': 'application/json' },
                type: 'GET'
            }).done(function (data) {
                try {
                    addTab(code, caption, data);
                    $(`#${code}_content_wrapper`)
                        .attr("data-source-url", url)
                        .attr("data-source-caption", caption || "")
                        .data("source-data", sourceData);
                    if (managementUserGuideEnabled) {
                        window.setTimeout(function () {
                        window.UserGuide?.startEligible?.();
                    }, 350);
                }
                }
                catch (e) {
                    appNotify({ message: e.message, type: 'error' });
                }
            }).fail(function (jqXHR, statusText, errorThrown) {
                if (jqXHR.status == 401) {
                    //window.location = "/Login";
                }
                else if (jqXHR.status == 403) {
                    //$.get("/AccessDenied")
                    //    .done(function (result) {
                    //        $("<div>").appendTo($("body")).dxPopup({
                    //            title: "No permission to view...", width: "30%", height: "50%",
                    //            contentTemplate: function () { return $("<div>").append(result); }
                    //        }).dxPopup("instance").show();
                    //    })
                    //    .fail(function (jqXHR, textStatus, errorThrown) {
                    //        appNotify({ message: `${errorThrown}. ${jqXHR.responseText}`, type: 'warning' });
                    //    });
                }
                else {
                    appNotify({ message: `${errorThrown}. ${jqXHR.responseText}`, type: 'warning' });
                }
            }).always(function () {
                console.log("Go to " + url);
            });
        }
        catch (err) {
            appErrorHandling('Library error: call callElementView was failed.', err);
            return;
        }
    }

    var replaceElementView = function (url, params, container) {
        try {
            if (url.length === 0) {
                appNotify({ message: "Page request does not exist.", type: 'info' });
                return false;
            }
            var routes = url.split('/');
            if (["null", "undefined"].indexOf(routes[routes.length - 1]) > -1) {
                appNotify({ message: "Incorrect web link, please contact Admin.", type: 'info' });
                return false;
            }
            $.ajax({
                url: addURLParams(url, params),
                headers: { 'Content-Type': 'application/json' },
                type: 'GET'
            }).done(function (data) {
                try {
                    container.html(data);
                }
                catch (e) {
                    appNotify({ message: e.message, type: 'error' });
                }
            }).fail(function (jqXHR, statusText, errorThrown) {
                if (jqXHR.status == 401) {
                    //window.location = "/Login";
                }
                else if (jqXHR.status == 403) {
                    //$.get("/AccessDenied")
                    //    .done(function (result) {
                    //        $("<div>").appendTo($("body")).dxPopup({
                    //            title: "No permission to view...", width: "30%", height: "50%",
                    //            contentTemplate: function () { return $("<div>").append(result); }
                    //        }).dxPopup("instance").show();
                    //    })
                    //    .fail(function (jqXHR, textStatus, errorThrown) {
                    //        appNotify({ message: `${errorThrown}. ${jqXHR.responseText}`, type: 'warning' });
                    //    });
                }
                else {
                    appNotify({ message: `${errorThrown}. ${jqXHR.responseText}`, type: 'warning' });
                }
            }).always(function () {
                //console.log("Replace " + url);
            });
        }
        catch (err) {
            appErrorHandling('Library error: call callElementView was failed.', err);
            return;
        }
    }

    var appendChildGridViewInsideAsync = function (childItem, itemElement, className = "MGridOption") {
        return new Promise((resolve, reject) => { // Trả về Promise
            try {
                try {
                    const GridClass = window[className]; // Lấy class từ global scope
                    if (!GridClass) {
                        throw new Error(`Class ${className} không tìm thấy!`);
                    }
                    var gridOptionConfig = {
                        refKey: childItem.refKey,
                        refField: childItem.refField,
                        height: _defaultGridMinHeight
                    };
                    var mGridOption = new GridClass(childItem.modelName, "User", gridOptionConfig);
                    mGridOption.refKey = childItem.refKey;
                    mGridOption.refField = childItem.refField;
                    mGridOption.gridEditorOptions = {};
                    mGridOption.gridEditorOptions = {
                        editing: childItem.editing
                    };
                    $(`<div id="dataGrid_${childItem.modelName}_${childItem.refKey}" style="min-height: ${_defaultGridMinHeight}px;">`).dxDataGrid(mGridOption.makeGridOptions(mGridOption)).appendTo(itemElement);
                    const dataGrid = $(`<div id="dataGrid_${childItem.modelName}_${childItem.refKey}" style="min-height: ${_defaultGridMinHeight}px;">`).dxDataGrid().dxDataGrid("instance");

                    if (dataGrid) {
                        resolve(dataGrid); // Trả về instance
                    } else {
                        reject("dxDataGrid instance không tìm thấy!");
                    }
                }
                catch (e) {
                    appNotify({ message: e.message, type: 'error' });
                    reject(e.message); // Trả lỗi khi xử lý trong switch
                }
            } catch (err) {
                appErrorHandling('Library error: call appendChildGridViewInsideAsync was failed.', err);
                reject(err.message);
            }
        });
    }

    var appendElementViewInside = function (url, params, container, code, typeChild) {
        try {
            if (url.length === 0) {
                appNotify({ message: "Page request does not exist.", type: 'info' });
                return false;
            }
            var routes = url.split('/');
            if (["null", "undefined"].indexOf(routes[routes.length - 1]) > -1) {
                appNotify({ message: "Incorrect web link, please contact Admin.", type: 'info' });
                return false;
            }
            $.ajax({
                url: addURLParams(url, params),
                headers: { 'Content-Type': 'application/json' },
                type: 'GET'
            }).done(function (data) {
                try {
                    switch (typeChild) {
                        case "appendTo":
                            var containerDiv = $(`<div id="${code}" style="height: 100%; width: 100%">`); // hardcode -> chỉnh lại
                            containerDiv.html(data);
                            containerDiv.appendTo(container);
                            break;
                        case "append":
                            var containerDiv = $(`<div id="${code}" style="height: 100%; width: 100%">`); // hardcode -> chỉnh lại
                            containerDiv.html(data);
                            container.append(containerDiv);
                            break;
                        default:
                            break;
                    }
                    return data;
                }
                catch (e) {
                    appNotify({ message: e.message, type: 'error' });
                }
            }).fail(function (jqXHR, statusText, errorThrown) {
                if (jqXHR.status == 401) {
                    //window.location = "/Login";
                }
                else if (jqXHR.status == 403) {
                    //$.get("/AccessDenied")
                    //    .done(function (result) {
                    //        $("<div>").appendTo($("body")).dxPopup({
                    //            title: "No permission to view...", width: "30%", height: "50%",
                    //            contentTemplate: function () { return $("<div>").append(result); }
                    //        }).dxPopup("instance").show();
                    //    })
                    //    .fail(function (jqXHR, textStatus, errorThrown) {
                    //        appNotify({ message: `${errorThrown}. ${jqXHR.responseText}`, type: 'warning' });
                    //    });
                }
                else {
                    appNotify({ message: `${errorThrown}. ${jqXHR.responseText}`, type: 'warning' });
                }
            }).always(function () {
                //console.log("Append " + url);
            });
        }
        catch (err) {
            appErrorHandling('Library error: call callElementView was failed.', err);
            return;
        }
    }

    var appendElementViewInsideAsync = function (url, params, container, code, typeChild) {
        return new Promise((resolve, reject) => { // Trả về Promise
            try {
                if (url.length === 0) {
                    appNotify({ message: "Page request does not exist.", type: 'info' });
                    reject("Page request does not exist.");
                    return;
                }
                var routes = url.split('/');
                if (["null", "undefined"].indexOf(routes[routes.length - 1]) > -1) {
                    appNotify({ message: "Incorrect web link, please contact Admin.", type: 'info' });
                    reject("Incorrect web link.");
                    return;
                }
                $.ajax({
                    url: addURLParams(url, params),
                    headers: { 'Content-Type': 'application/json' },
                    type: 'GET'
                }).done(function (data) {
                    try {
                        var containerDiv = $(`<div id="${code}" class="container" style="height: 100%; width: 100%">`);
                        switch (typeChild) {
                            case "appendTo":
                                containerDiv.html(data);
                                containerDiv.appendTo(container);
                                break;
                            case "append":
                                containerDiv.html(data);
                                container.append(containerDiv);
                                break;
                            default:
                                break;
                        }

                        // Tìm và tạo dxForm instance sau khi script thực thi
                        try {
                            const formInstance = $(`#form_${code}`).dxForm().dxForm("instance");
                            formInstance.option("container", containerDiv);
                            if (formInstance) {
                                resolve(formInstance); // Trả về instance
                            } else {
                                const customError = {
                                    status: 500,
                                    statusText: "Form Not Found",
                                    responseText: `dxForm instance với id "form_${code}" không tìm thấy.`,
                                    readyState: 4,
                                    responseJSON: {
                                        typeMsg: ["popup"],
                                        message: [`Không tìm thấy form: form_${code}`]
                                    }
                                };
                                reject(customError);
                                //throw new Error("dxForm instance không tìm thấy!");
                            }

                        }
                        catch (exx) {
                            resolve(null);
                        }
                        //resolve(data); // Trả về dữ liệu từ AJAX
                    }
                    catch (e) {
                        appNotify({ message: e.message, type: 'error' });
                        reject(e.message); // Trả lỗi khi xử lý trong switch
                    }
                }).fail(function (jqXHR, statusText, errorThrown) {
                    if (jqXHR.status == 401) {
                        //window.location = "/Login";
                    } else if (jqXHR.status == 403) {
                        //$.get("/AccessDenied")
                        //    .done(function (result) {
                        //        $("<div>").appendTo($("body")).dxPopup({
                        //            title: "No permission to view...", width: "30%", height: "50%",
                        //            contentTemplate: function () { return $("<div>").append(result); }
                        //        }).dxPopup("instance").show();
                        //    })
                        //    .fail(function (jqXHR, textStatus, errorThrown) {
                        //        appNotify({ message: `${errorThrown}. ${jqXHR.responseText}`, type: 'warning' });
                        //    });
                    } else {
                        appNotify({ message: `${errorThrown}. ${jqXHR.responseText}`, type: 'warning' });
                    }
                    reject(`${errorThrown}. ${jqXHR.responseText}`); // Trả lỗi từ AJAX
                }).always(function () {
                    //console.log("Append " + url);
                });
            } catch (err) {
                appErrorHandling('Library error: call callElementView was failed.', err);
                reject(err.message);
            }
        });
    };




    //function closeTab() {
    //    var li = $("#tablist > ul").find("[tabindex = '0']");
    //    var panelId = li.remove().attr("aria-controls");
    //    $("#" + panelId).remove();
    //    $("#tablist").tabs("refresh");
    //}



    function initTabs() {
        $("#tablist").tabs({
            // tab click
            activate: function (event, ui) {
                try {
                    var $tabContent = ui.newPanel;
                    var dataGrid = $try(function () {
                        return $tabContent.find("[id^='dataGrid_']").dxDataGrid("instance");
                    });
                    if (dataGrid != null) {
                        //dataGrid.option("height", "100%");
                        //dataGrid.refresh();
                    }
                    var parentElement = $($tabContent).parent();
                    parentElement.css({zIndex: 10000});
                    var $oldPanel = ui.oldPanel;
                     var oldparentElement = $($oldPanel).parent();
                     oldparentElement.css({zIndex: 0});
                } catch (err) {
                    appErrorHandling('Library error: call onTabActivate was failed.', err);
                    return;
                }
            }
        });
    }

    var tabFormatButtonClass = 'fa fa-times close-tab-btn';
    var addTab = function (code, tabTitle = '...', tabContent) {
        try {
            var $tabs = $("#tablist");
            const tabExisted = $tabs.find(`#${code}_content_wrapper`).length;
            if (!tabExisted) {
                createNewTabElement(code, tabTitle, $tabs, tabContent);
            }
            else {
            }
            var tabIndex = $tabs.find(`#${code}_content_wrapper`).index() - 1;
            $tabs.tabs("option", "active", tabIndex);
            $tabs.find(`#${code}`).html(tabContent);
        } catch (err) {
            appErrorHandling('Library error: call addTab was failed.', err);
        }
    };

    var createNewTabElement = function (code, tabTitle = '...', $tabs, tabContent) {
        const li = $(`
                    <li id="${code}_li_tab" class="expand-ltr">
                        <div id="${code}_tab" style="display: flex;float: left;"><a href="#${code}" style="padding: 7px;">${tabTitle}</a>
                        <span id="${code}_closeTab" class='${tabFormatButtonClass}' data-code="${code}_closeTab"></span></div>
                    </li>
                `);//.attr("aria-controls", code);
        //<span id="${code}_progressBar" style="padding: 5px;"></span>
        var $divContentWrapper = $(`<div id="${code}_content_wrapper" class="content-wrapper" style="z-index:0">`);
        var $tabContainer = $(`<div id="${code}">`);
        $tabContainer.appendTo($divContentWrapper);
        $tabs.find(".ui-tabs-nav").append(li);
        //var $contentDiv = $(`<div></div>`).html(tabContent);
        //$contentDiv.appendTo($tabContainer);
        $tabs.append($divContentWrapper);
        $tabs.tabs("refresh");
    }
    //deactivate function 20260623
    // var removeTab = function (activeId) {
    //     try {
    //         var li = $("#tablist > ul").find("[tabindex = '0']");
    //         var panelId = li.remove().attr("aria-controls");
    //         $("#" + panelId).remove();



    //         var tabIndex = ($("#" + activeId).index()) - 1;
    //         if (tabIndex >= 0) {
    //             $("#tablist").tabs("option", "active", tabIndex);
    //         }
    //             $("#tablist").tabs("refresh");
    //     } catch (err) {
    //         appErrorHandling('Library error: call removeTab was failed.', err);
    //         return;
    //     }
    // };

    var nativeRemoveTabV2 = function(formId){

        var $tabDivId = $(`li#${formId}_li_tab`);
                                                $tabDivId.animate({
                                                    width: '0px',
                                                    opacity: '0'
                                                }, 220);
                                                setTimeout(function () {
                                                    var $li = $tabDivId.closest("li");
                                                    var panelId = $li.attr("aria-controls");
                                                    $li.remove();

                                                    if (panelId) {
                                                        $("#" + panelId).remove();
                                                        $(`#${panelId}_content_wrapper`).remove();
                                                        document.body.classList.remove("hasRightPanel");
                                                        $("#btnCloseRightComment").click();
                                                    }
                                                    $("#tablist").tabs("refresh");
                                                }, 150);
                                                };


    var nativeRemoveTab = function($tabDivId){
        $tabDivId.animate({
            width: '0px',
            opacity: '0'
        }, 220);
        setTimeout(function () {
            var $li = $tabDivId.closest("li");
            var panelId = $li.attr("aria-controls");
            $li.remove();

            if (panelId) {
                $("#" + panelId).remove();
                $(`#${panelId}_content_wrapper`).remove();
                document.body.classList.remove("hasRightPanel");
                $("#btnCloseRightComment").click();
                //document.documentElement.style.setProperty("--collapsed-form-mr", "-26%");
            }
            $("#tablist").tabs("refresh");
        }, 150); // === duration animate
    }

    var setTabName = function (name, code) {
        try {
            var li = $("#tablist > ul").find("[tabindex = '0']");
            if (li.length > 0) {
                var a = li.find("a");
                //set tab name
                if (name == null)
                    a.text("undefined");
                else
                    a.text(name);

                //set href
                a[0].setAttribute("href", `#${code}`);
                //set aria-controls
                li[0].setAttribute("aria-controls", code);
                //set div id
                var div = $("#tablist").find(`[aria-hidden = 'false']`);
                div[0].setAttribute("id", code);
            }
        } catch (err) {
            appErrorHandling('Library error: call setTabName was failed.', err);
            return;
        }
    };

    var tabOverflowUpdateFrame = 0;

    function renderTabOverflowMenu() {
        var $tabs = $("#tablist");
        var $menu = $("#tabOverflowMenu");
        if (!$tabs.length || !$menu.length) return;

        $menu.empty();
        $tabs.children("ul").children("li").each(function (index) {
            var $tab = $(this);
            var title = $tab.find("a").first().text().trim() || `Tab ${index + 1}`;
            var $item = $("<button>", {
                type: "button",
                class: "tab-overflow-item",
                title: title,
                role: "menuitem",
                "data-tab-index": index
            });

            $("<i>", {
                class: "fa fa-file-o tab-overflow-item-icon",
                "aria-hidden": "true"
            }).appendTo($item);
            $("<span>", {
                class: "tab-overflow-item-title",
                text: title
            }).appendTo($item);
            $("<i>", {
                class: "fa fa-check tab-overflow-item-check",
                "aria-hidden": "true"
            }).appendTo($item);

            if ($tab.hasClass("ui-tabs-active")) {
                $item.addClass("is-active").attr("aria-current", "page");
            }
            $item.appendTo($menu);
        });
    }

    function updateTabOverflow() {
        tabOverflowUpdateFrame = 0;
        var $tabList = $("#tablist");
        var $nav = $tabList.children("ul");
        var $overflow = $("#tabOverflow");
        if (!$tabList.length || !$nav.length || !$overflow.length) return;

        var isVisible = !$overflow.prop("hidden");
        var recoveredWidth = isVisible ? ($overflow.outerWidth(true) || 0) + 4 : 0;
        var availableWithoutButton = $nav[0].clientWidth + recoveredWidth;
        var needsOverflow = $nav[0].scrollWidth > availableWithoutButton + 1;

        $overflow.prop("hidden", !needsOverflow);
        if (!needsOverflow) {
            $("#tabOverflowMenu").prop("hidden", true);
            $("#tabOverflowButton").attr("aria-expanded", "false");
        } else if (!$("#tabOverflowMenu").prop("hidden")) {
            renderTabOverflowMenu();
        }
    }

    function scheduleTabOverflowUpdate() {
        if (tabOverflowUpdateFrame) cancelAnimationFrame(tabOverflowUpdateFrame);
        tabOverflowUpdateFrame = requestAnimationFrame(updateTabOverflow);
    }

    function initTabOverflow() {
        var $nav = $("#tablist").children("ul");
        var $button = $("#tabOverflowButton");
        var $menu = $("#tabOverflowMenu");
        if (!$nav.length || !$button.length || !$menu.length) return;

        $button.off("click.tabOverflow").on("click.tabOverflow", function (event) {
            event.stopPropagation();
            var willOpen = $menu.prop("hidden");
            if (willOpen) renderTabOverflowMenu();
            $menu.prop("hidden", !willOpen);
            $button.attr("aria-expanded", willOpen ? "true" : "false");
        });

        $menu.off("click.tabOverflow").on("click.tabOverflow", ".tab-overflow-item", function () {
            var tabIndex = Number($(this).attr("data-tab-index"));
            if (!Number.isInteger(tabIndex)) return;

            $("#tablist").tabs("option", "active", tabIndex);
            var tabElement = $nav.children("li").get(tabIndex);
            tabElement?.scrollIntoView({ behavior: "smooth", block: "nearest", inline: "nearest" });
            $menu.prop("hidden", true);
            $button.attr("aria-expanded", "false");
        });

        $(document)
            .off("click.tabOverflow keydown.tabOverflow")
            .on("click.tabOverflow", function () {
                $menu.prop("hidden", true);
                $button.attr("aria-expanded", "false");
            })
            .on("keydown.tabOverflow", function (event) {
                if (event.key !== "Escape") return;
                $menu.prop("hidden", true);
                $button.attr("aria-expanded", "false");
                $button.trigger("focus");
            });

        new MutationObserver(scheduleTabOverflowUpdate).observe($nav[0], {
            childList: true,
            subtree: true,
            characterData: true,
            attributes: true,
            attributeFilter: ["class"]
        });

        if (window.ResizeObserver) {
            new ResizeObserver(scheduleTabOverflowUpdate).observe($(".management-topbar")[0]);
        } else {
            $(window).off("resize.tabOverflow").on("resize.tabOverflow", scheduleTabOverflowUpdate);
        }

        scheduleTabOverflowUpdate();
    }
</script>
<link href="~/favicon_new.ico" rel="icon" type="image/x-icon" />
<script src="~/lib/AdminLTE/adminlte.min.js"></script>
<body class="sidebar-mini skin-re" style="height: auto; min-height: 100%; width: 100%;">
    <header>
        <div class="main-header wrapper management-topbar">
            <!-- LOGO -->
            <div class="management-logo-slot">
                <div class="pull-left management-logo-inner">
                    <a href="#" class="logo management-logo-link" data-toggle="push-menu" role="button">
                        <div class="logo-lg"><b>Workflow Management</b></div>
                        <div class="logo-mini" style=""><b></b></div>
                    </a>
                </div>
            </div>
            <!--Tab list-->
            <div id="tablist" class="ui-tabs-nav">
                <ul></ul>
            </div>

            <div id="tabOverflow" class="tab-overflow" hidden>
                <button id="tabOverflowButton"
                        class="tab-overflow-button"
                        type="button"
                        title="Show open tabs"
                        aria-label="Show open tabs"
                        aria-haspopup="menu"
                        aria-expanded="false">
                    <i class="fa fa-angle-down" aria-hidden="true"></i>
                </button>
                <div id="tabOverflowMenu" class="tab-overflow-menu" role="menu" hidden></div>
            </div>

            <!-- Global maintenance status. Per-form debug controls live in each
                 form's Maintenance Info panel so multiple tabs never share IDs. -->
            <div id="managementMaintenance" class="management-maintenance" aria-label="Maintenance status">
                @if (string.Equals(ViewData["NotifyEnv"]?.ToString(), "true", StringComparison.OrdinalIgnoreCase))
                {
                    <div id="environmentLabel"
                         class="management-environment-badge"
                         tabindex="0"
                         role="button"
                         aria-haspopup="dialog"
                         aria-expanded="false"
                         aria-controls="environmentPanel">
                        <i class="fa fa-server" aria-hidden="true"></i>
                        <span>Environment</span>
                        <strong id="environmentValue">@ViewData["Environment"]</strong>
                        <i class="fa fa-chevron-down management-environment-info-icon" aria-hidden="true"></i>
                        <div id="environmentPanel" class="management-environment-hover" role="dialog" aria-label="Development diagnostics">
                            <strong>Development diagnostics</strong>
                            <div class="management-environment-summary">
                                <div class="management-environment-summary-row">
                                    <span>Environment</span>
                                    <b>@ViewData["Environment"]</b>
                                </div>
                            </div>
                            <div id="homeRoleSwitcher"
                                 class="home-role-switcher management-environment-role-row"
                                 title="Switch workflow role for this session"
                                 aria-label="Switch workflow role for this session"
                                 aria-hidden="true">
                                <span>Debug role</span>
                                <div id="homeRoleSelect"></div>
                            </div>
                            <div id="homeBranchSwitcher"
                                 class="home-role-switcher management-environment-role-row"
                                 title="Switch site office for this debug session"
                                 aria-label="Switch site office for this debug session"
                                 aria-hidden="true">
                                <span>Debug site office</span>
                                <div id="homeBranchSelect"></div>
                            </div>
                            <div class="management-environment-hover-row">
                                <span>SignalR</span>
                                <div id="signalRMonitor" class="signalr-monitor signalr-disconnected">
                                    <span class="signalr-dot"></span>
                                    <span class="signalr-text">Offline</span>
                                </div>
                            </div>
                            <div id="signalRCounter" class="management-signalr-counter">Session count from SignalR: (0)</div>
                            <div id="signalRLazyLoadStatus" class="management-signalr-task is-idle">
                                <span>FetchEnum (lazy load)</span>
                                <b>Idle</b>
                                <small>Waiting for enum batch</small>
                            </div>                            <div class="management-signalr-detail-title">Called handlers</div>
                            <div id="signalRFunctionDetails" class="management-signalr-details">
                                <span>No SignalR handler called yet.</span>
                            </div>
                        </div>
                    </div>
                }
                <div id="returnToSuperUser" class="management-return-admin"></div>
            </div>

            <!--Login Info-->
            <div class="management-header-actions">
                <div id="btnNew" style="display:none"></div>
                @if (string.Equals(ViewData["IsSuperUser"]?.ToString(), "true", StringComparison.OrdinalIgnoreCase))
                {
                    <button id="userGuideLauncher"
                            class="ug-guide-launcher"
                            type="button"
                            title="Set up user guides"
                            aria-label="Set up user guides">
                        <i class="fa fa-compass" aria-hidden="true"></i>
                    </button>
                }
                <button id="tmivTourGuideLauncher"
                        class="ug-guide-launcher"
                        type="button"
                        title="Start UI tour"
                        aria-label="Start UI tour">
                    <i class="fa fa-map-signs" aria-hidden="true"></i>
                </button>
                <button id="notificationBtn" class="notif-wrapper square-btn">
                    <svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" width="30" height="30" viewBox="0 0 256 256" xml:space="preserve">
                    <g style="stroke: none; stroke-width: 0; stroke-dasharray: none; stroke-linecap: butt; stroke-linejoin: miter; stroke-miterlimit: 10; fill: none; fill-rule: nonzero; opacity: 1;" transform="translate(1.4065934065934016 1.4065934065934016) scale(2.81 2.81)">
                    <path d="M 83.25 74.548 H 6.75 c -1.536 0 -2.864 -0.988 -3.306 -2.457 c -0.441 -1.468 0.122 -3.022 1.401 -3.868 c 0.896 -0.594 1.954 -1.152 3.233 -1.707 c 5.52 -2.514 6.42 -16.025 7.144 -26.882 c 0.182 -2.74 0.355 -5.327 0.59 -7.664 c 1.926 -12.752 8.052 -20.942 18.223 -24.424 C 35.767 3.067 40.169 0 45 0 s 9.233 3.067 10.964 7.546 c 10.171 3.482 16.298 11.671 18.214 24.352 c 0.245 2.409 0.416 4.996 0.6 7.736 c 0.723 10.857 1.624 24.368 7.168 26.893 c 1.255 0.544 2.313 1.102 3.21 1.696 c 1.279 0.846 1.842 2.4 1.4 3.868 C 86.114 73.56 84.785 74.548 83.25 74.548 z M 45 2.934 c -3.818 0 -7.279 2.556 -8.416 6.215 l -0.228 0.733 l -0.732 0.231 c -9.568 3.018 -15.096 10.287 -16.9 22.224 c -0.221 2.216 -0.392 4.779 -0.573 7.493 c -0.816 12.242 -1.74 26.117 -8.88 29.368 c -1.129 0.49 -2.064 0.982 -2.806 1.473 c -0.265 0.175 -0.26 0.409 -0.21 0.575 c 0.051 0.168 0.177 0.368 0.496 0.368 h 76.5 c 0.318 0 0.445 -0.2 0.496 -0.368 c 0.05 -0.166 0.054 -0.4 -0.209 -0.575 h -0.001 c -0.741 -0.491 -1.677 -0.983 -2.782 -1.462 c -7.163 -3.261 -8.088 -17.137 -8.905 -29.379 c -0.181 -2.714 -0.352 -5.277 -0.582 -7.565 c -1.795 -11.864 -7.323 -19.134 -16.891 -22.151 l -0.732 -0.231 L 53.416 9.15 C 52.279 5.49 48.818 2.934 45 2.934 z" style="stroke: none; stroke-width: 1; stroke-dasharray: none; stroke-linecap: butt; stroke-linejoin: miter; stroke-miterlimit: 10; fill: var(--c-rgb-0-0-0); fill-rule: nonzero; opacity: 1;" transform=" matrix(1 0 0 1 0 0) " stroke-linecap="round" />
                    <path d="M 33.257 78.292 C 33.277 84.75 38.536 90 45 90 c 6.463 0 11.723 -5.25 11.743 -11.708 H 33.257 z M 45 87.066 c -3.816 0 -7.063 -2.443 -8.285 -5.843 h 16.57 C 52.063 84.623 48.816 87.066 45 87.066 z" style="stroke: none; stroke-width: 1; stroke-dasharray: none; stroke-linecap: butt; stroke-linejoin: miter; stroke-miterlimit: 10; fill: var(--c-rgb-0-0-0); fill-rule: nonzero; opacity: 1;" transform=" matrix(1 0 0 1 0 0) " stroke-linecap="round" />
                    </g>
                    </svg>
                    <span class="notif-dot"></span>
                    <span class="notif-count" id="notifCount" style="display:none;">0</span>
                </button>
                <div class="navbar-nav user user-menu">
                    <input id="rolename" type="hidden" />
                    <button id="userAccountTrigger" class="user-account-trigger" type="button" aria-haspopup="true" aria-expanded="false">
                        <span id="accountAvatarInitial" class="user-account-avatar">U</span>
                        <span class="user-account-copy">
                            <span id="accountLabel" class="user-account-name">User</span>
                            <span id="accountRoleBadge" class="user-account-role">Role not selected</span>
                        </span>
                        <i class="fa fa-angle-down user-account-chevron" aria-hidden="true"></i>
                    </button>
                </div>

                <div id="dropdown-menu" class="user-account-menu" style="display:none;">
                    <div class="user-menu-heading">
                        @* <span id="menuAvatarInitial" class="user-menu-avatar">U</span> *@
                        <div>
                            <strong id="displayLabel">User account</strong>
                            <span class="user-login-detail">Site Office: <b id="userSiteOffice">-</b></span>
                            <span id="roleLabel">Phòng ban chưa chọn</span>
                        </div>
                    </div>
                    <button id="openUserSettings" class="user-settings-entry" type="button">
                        <span class="user-settings-icon"><i class="fa fa-cog"></i></span>
                        <span><strong>Settings</strong><small>Role, theme and notifications</small></span>
                        <i class="fa fa-angle-right"></i>
                    </button>
                </div>

            </div>
            @* <p id="onlineState" class="status-button"> </p> *@

        </div>

        @await Html.PartialAsync("_SPAReference")
        <script>
            if (managementUserGuideEnabled) {
            window.UserGuide?.bindLauncher("#userGuideLauncher", {
                adminOnly: true,
                allowManage: true,
                autoStart: false
            });
            window.UserGuide?.bindLauncher("#tmivTourGuideLauncher", {
                allowManage: false,
                autoStart: true
            });
             } else {
                    $("#userGuideLauncher, #tmivTourGuideLauncher").hide();
                }
        </script>
        <div id="mainPopup"></div>
        <div id="outlinePopup"></div>
        <div id="loadingPopup"></div>
        <div id="inputTextPopup"></div>
        <div id="detailPopup"></div>
        <div id="notificationPopup"></div>
        <div id="notificationPopOver"></div>
        <div id="assigneeEntryForm"></div>
        <div id="moduleLauncherPopup"></div>
        <div id="userSettingsModal" class="user-settings-modal" aria-hidden="true" hidden>
            <div class="user-settings-backdrop"></div>
            <section class="user-settings-dialog" role="dialog" aria-modal="true" aria-labelledby="userSettingsTitle">
                <header class="user-settings-header">
                    <div>
                        <span class="user-settings-kicker">ACCOUNT SETTINGS</span>
                        <h2 id="userSettingsTitle">Account settings</h2>
                        <p>Choose your workflow role, interface theme and notification experience.</p>
                    </div>
                    <button id="closeUserSettings" class="user-settings-close" type="button" aria-label="Close settings">&times;</button>
                </header>
                <div id="userRoleOptions" class="user-role-options"></div>
                <section class="account-theme-settings" aria-labelledby="accountThemeSettingTitle">
                    <div class="account-setting-heading">
                        <div class="account-theme-icon"><i class="fa fa-paint-brush"></i></div>
                        <div>
                            <strong id="accountThemeSettingTitle">Interface theme</strong>
                            <span>Preview a theme now; it will be remembered for this account after saving.</span>
                        </div>
                    </div>
                    <div id="accountThemeOptions" class="account-theme-options">
                        <label class="account-theme-card" data-theme-option="reactify">
                            <input type="radio" name="accountTheme" value="reactify" />
                            <span class="account-theme-preview account-theme-preview-reactify" aria-hidden="true">
                                <i></i><b></b><em></em>
                            </span>
                            <span><strong>Reactify Light</strong><small>Slate, blue and teal</small></span>
                            <i class="fa fa-check account-theme-check"></i>
                        </label>
                        <label class="account-theme-card" data-theme-option="classic">
                            <input type="radio" name="accountTheme" value="classic" />
                            <span class="account-theme-preview account-theme-preview-classic" aria-hidden="true">
                                <i></i><b></b><em></em>
                            </span>
                            <span><strong>TMIV Classic</strong><small>Original blue and red</small></span>
                            <i class="fa fa-check account-theme-check"></i>
                        </label>
                    </div>
                </section>
                <section class="account-notification-settings" aria-labelledby="chromeNotificationSettingTitle">
                    <div class="account-notification-icon"><i class="fa fa-bell"></i></div>
                    <div class="account-notification-copy">
                        <strong id="chromeNotificationSettingTitle">Chrome notifications</strong>
                        <span>Show workflow alerts through the browser. When disabled or unavailable, TMIVCom Notification will be used.</span>
                        <small id="chromeNotificationPermissionStatus"></small>
                    </div>
                    <label class="account-setting-switch" title="Enable Chrome notifications">
                        <input id="enableChromeNotifications" type="checkbox" />
                        <span class="account-setting-slider"></span>
                    </label>
                </section>
                <div class="user-settings-note">
                    <i class="fa fa-info-circle"></i>
                    Menu permissions will be fully refreshed the next time this page is loaded.
                </div>
                <footer class="user-settings-footer">
                    <button id="cancelUserSettings" class="user-settings-cancel" type="button">Cancel</button>
                    <button id="saveUserSettings" class="user-settings-save" type="button">
                        <i class="fa fa-check"></i> Save settings
                    </button>
                </footer>
            </section>
        </div>

    </header>
    @await Html.PartialAsync("_MenuPartial")

    <!-- Content Wrapper. Contains page content -->
    @* <div id="content-wrapper" class="content-wrapper" style="min-height: 901px; margin-top:0px;">
        </div> 
 *@
    <div id="dashboard" style="margin-left: 10%;
    margin-top: 10%;
    width: 100%;
    height: 100%;
    justify-content: center;
    display: flex;">
        <img src="~/img/TMIV_Logo.jpg"
             aria-hidden="true" alt="Icon image" style="opacity:0.5;width:400px">
    </div>
</body>
<script type="text/javascript">
    // function monitorConnection(url, buttonId, interval = 5000) {
        // checkConnection(url, interval).then(className => {
        //         updateButtonStatus(buttonId, className);
        //     });
        // }

        // removed this as not need any more => var appLoadingPanel = $("<div>").appendTo($(constNotifyContainer)).dxLoadPanel({ visible: false }).dxLoadPanel("instance");
        initTabs();
        initTabOverflow();

        //Close tab button function
        $("#tablist").on("click", "span.fa.fa-times", function () {
            var $tabDivId = $(this).closest("[id$='_li_tab']");
            nativeRemoveTab($tabDivId);
        });

        // Chrome controls the wording of this dialog. It is intentionally
        // registered for the whole main screen so closing/reloading the actual
        // browser tab always requests confirmation after user interaction.
        window.addEventListener("beforeunload", function (event) {
            event.preventDefault();
            event.returnValue = true;
            return true;
        });

        window.addEventListener("unload", () => {
            navigator.sendBeacon(
                "/api/log-hours",
                    JSON.stringify({
                totalHours: calculateTotalHours()
                })
            );
        });

        _isSuperUser = @Html.Raw(ViewData["IsSuperUser"]);
        _isDebugMode = `@Html.Raw(ViewData["IsDebugMode"])`;

        (function initializeHomeRoleSwitcher() {
            const canSwitchRole = String(_isDebugMode).toLowerCase() === "true"
                || String(_isSuperUser).toLowerCase() === "true";
            const $switcher = $("#homeRoleSwitcher").toggleClass("is-visible", canSwitchRole);
            const $select = $("#homeRoleSelect");
            const roles = qtDeptModuleCode.slice();
            let syncingRole = false;

            function notifyOpenWorkflowViews(role) {
                $(`[id^='${viewIdModulePrefix.quotation}-overviewRoot_'], ` +
                    `[id^='${viewIdModulePrefix.policyIssuance}-overviewRoot_']`).each(function () {
                    const match = String(this.id || "").match(/^(qt|pi)-overviewRoot_(.+)$/);
                    if (!match) return;
                    $(document).trigger("overviewMaintenanceRoleChanged", [{
                        moduleCode: match[1],
                        recordId: match[2],
                        role: role,
                        source: "home"
                    }]);
                });
            }

            window.syncHomeRoleSwitcher = function (role) {
                const normalizedRole = String(role || "").trim().toUpperCase();
                const instance = $select.hasClass("dx-selectbox")
                    ? $select.dxSelectBox("instance")
                    : null;
                if (!instance || !roles.includes(normalizedRole)) return;
                syncingRole = true;
                instance.option("value", normalizedRole);
                syncingRole = false;
            };

            if (!canSwitchRole || !$select.length) return;

            $select.dxSelectBox({
                dataSource: roles,
                value: roles.includes(String(_role || "").trim().toUpperCase())
                    ? String(_role).trim().toUpperCase()
                    : null,
                placeholder: "Role",
                stylingMode: "outlined",
                width: 76,
                searchEnabled: false,
                showClearButton: false,
                onValueChanged: function (e) {
                    if (syncingRole || !e.value) return;
                    _role = e.value;
                    window._role = e.value;
                    notifyOpenWorkflowViews(e.value);
                    window.refreshRouteCommentPermissions?.();
                    DevExpress.ui.notify("Role switched: " + e.value, "info", 1200);
                }
            });

            window.syncHomeRoleSwitcher(_role);
            $switcher.attr("aria-hidden", "false");
        })();

        (function initializeHomeBranchSwitcher() {
            const canSwitchBranch = String(_isDebugMode).toLowerCase() === "true"
                || String(_isSuperUser).toLowerCase() === "true";
            const $switcher = $("#homeBranchSwitcher").toggleClass("is-visible", canSwitchBranch);
            const $select = $("#homeBranchSelect");
            const branches = ["HCM", "HN"];
            let syncingBranch = false;

            function normalizeBranch(branch) {
                return String(branch || "").trim().toUpperCase();
            }

            window.syncHomeBranchSwitcher = function (branch) {
                const normalizedBranch = normalizeBranch(branch);
                const instance = $select.hasClass("dx-selectbox")
                    ? $select.dxSelectBox("instance")
                    : null;
                if (!instance) return;
                syncingBranch = true;
                instance.option("value", branches.includes(normalizedBranch) ? normalizedBranch : null);
                syncingBranch = false;
            };

            if (!canSwitchBranch || !$select.length) return;

            $select.dxSelectBox({
                dataSource: branches,
                value: branches.includes(normalizeBranch(_branch)) ? normalizeBranch(_branch) : null,
                placeholder: "Office",
                stylingMode: "outlined",
                width: 92,
                searchEnabled: false,
                showClearButton: false,
                onValueChanged: function (e) {
                    if (syncingBranch || !e.value) return;
                    _branch = e.value;
                    window._branch = e.value;
                    window.syncLoginUserSiteOffice?.(e.value);
                    $(document).trigger("debugSiteOfficeChanged", [{
                        branch: e.value,
                        source: "home"
                    }]);
                    DevExpress.ui.notify("Site office switched: " + e.value, "info", 1200);
                }
            });

            window.syncHomeBranchSwitcher(_branch);
            $switcher.attr("aria-hidden", "false");
        })();

        var _notifyEnv = `@Html.Raw(ViewData["NotifyEnv"])`;
        if (_notifyEnv == "true") {
            $("#environmentLabel").addClass("is-visible");
        }
        window.syncLoginUserSiteOffice = function (branch) {
            const office = String(branch || "").trim();
            $("#userSiteOffice").text(office || "-");
        };
        window.syncLoginUserSiteOffice(_branch);

        (function initializeEnvironmentPanel() {
            const $trigger = $("#environmentLabel");
            const $panel = $("#environmentPanel");
            if (!$trigger.length || !$panel.length) return;

            function setEnvironmentPanelOpen(isOpen) {
                $trigger.toggleClass("is-open", isOpen).attr("aria-expanded", String(isOpen));
            }

            $trigger.on("click", function (event) {
                if ($(event.target).closest("#environmentPanel").length) return;
                setEnvironmentPanelOpen(!$trigger.hasClass("is-open"));
            });
            $trigger.on("keydown", function (event) {
                if (event.target !== this || !["Enter", " "].includes(event.key)) return;
                event.preventDefault();
                setEnvironmentPanelOpen(!$trigger.hasClass("is-open"));
            });
            $panel.on("click", function (event) {
                event.stopPropagation();
            });
            $(document).on("click.environmentPanel", function (event) {
                if ($(event.target).closest("#environmentLabel, .dx-overlay-wrapper").length) return;
                setEnvironmentPanelOpen(false);
            });
            $(document).on("keydown.environmentPanel", function (event) {
                if (event.key === "Escape") setEnvironmentPanelOpen(false);
            });
        })();
        // if (_notifyEnv  == "true")
        //     $("#environmentJogetLabel").text(`(Joget Environment: @ViewData["JogetEnvironment"])`);
        if (_isDebugMode == "true"){


            $("#returnToSuperUser").dxButton({
                 text: "Back To Admin",
                 stylingMode: "contained",
                 type: "normal",
                 onClick: function () {
                     $.ajax({
                         url: `/api/Users/ReturnToAccount`,
                         method: "POST",
                         success: function (response) {
                             appNotify({ message: response.message, type: "success" });
                             setTimeout(function () {
                                 window.location.replace(`${response.redirectUrl || "/Management"}?returned=${Date.now()}`);
                             }, 400);
                         },
                         error: function (err) {
                             appNotify({
                                 message: err.responseJSON?.message || "Unable to return to the original account.",
                                 type: "error"
                             });
                         }
                     });
                 },
            });
        }


            var loadParams = `@Html.Raw(@ViewData["LoadParams"])`;
            var validJsonString = loadParams.slice(1, -1);
            if (loadParams != "{}" && loadParams != "" && loadParams != undefined && loadParams != null) {
                invokePageTab(validJsonString);
                // var obj = JSON.parse(validJsonString);
                //   setTimeout(() => {
                //     callElementView(obj.url, obj.caption, obj.name, null)
                //     window.history.replaceState(null, '', '/Management#');
                //    }, 1000);
            }

    const fallbackRoleDescriptions = {
        FO: "Front Office - tiếp nhận yêu cầu và phối hợp với khách hàng.",
        TS: "Technical Services - chuẩn bị và rà soát nội dung kỹ thuật.",
        PM: "Product Management - quản lý sản phẩm và điều khoản.",
        UW: "Underwriting - đánh giá rủi ro và quyết định nhận bảo hiểm.",
        LMKT: "Local Marketing - điều phối kinh doanh và phát hành đơn."
    };
    const fallbackRoleOptions = [...new Set([...qtDeptModuleCode, ...piDeptModuleCode])]
        .map(code => ({ code, description: fallbackRoleDescriptions[code] || code }));
    let selectedUserRole = "";
    let userRoleOptions = fallbackRoleOptions;
    const chromeNotificationStoragePrefix = "tmiv.chromeNotifications.enabled:";
    let currentSettingsAccount = "";
    let chromeNotificationsEnabled = true;
    const allowedUserThemes = ["reactify", "classic"];
    const themeStoragePrefix = "tmiv.theme:";
    let savedUserTheme = window.tmivTheme || "reactify";
    let selectedUserTheme = savedUserTheme;
    window.tmivChromeNotificationsEnabled = true;

    function getThemeStorageKey() {
        const account = String(currentSettingsAccount || _loginUser || "anonymous")
            .trim()
            .toLowerCase();
        return `${themeStoragePrefix}${account}`;
    }

    function readThemePreference() {
        try {
            const accountTheme = localStorage.getItem(getThemeStorageKey());
            const globalTheme = localStorage.getItem("tmiv.theme");
            const theme = accountTheme || globalTheme || "reactify";
            return allowedUserThemes.includes(theme) ? theme : "reactify";
        } catch {
            return "reactify";
        }
    }

    function renderThemePreference() {
        $("input[name='accountTheme']").each(function () {
            const isSelected = this.value === selectedUserTheme;
            $(this).prop("checked", isSelected);
            $(this).closest(".account-theme-card").toggleClass("is-selected", isSelected);
        });
    }

    function applyThemePreference(theme, persist) {
        const nextTheme = allowedUserThemes.includes(theme) ? theme : "reactify";
        selectedUserTheme = nextTheme;
        document.documentElement.dataset.appTheme = nextTheme;
        window.tmivTheme = nextTheme;
        renderThemePreference();
        if (persist) {
            savedUserTheme = nextTheme;
            try {
                localStorage.setItem("tmiv.theme", nextTheme);
                localStorage.setItem(getThemeStorageKey(), nextTheme);
            } catch (error) {
                console.warn("Unable to save interface theme preference.", error);
            }
        }
    }

    function getChromeNotificationStorageKey() {
        const account = String(currentSettingsAccount || _loginUser || "anonymous")
            .trim()
            .toLowerCase();
        return `${chromeNotificationStoragePrefix}${account}`;
    }

    function readChromeNotificationPreference() {
        try {
            const storedValue = localStorage.getItem(getChromeNotificationStorageKey());
            return storedValue === null ? true : storedValue === "true";
        } catch {
            return true;
        }
    }

    function updateChromeNotificationPermissionStatus() {
        const $status = $("#chromeNotificationPermissionStatus");
        if (!("Notification" in window)) {
            $status.text("Chrome notifications are unavailable; TMIVCom Notification will be used.");
            return;
        }

        if (!chromeNotificationsEnabled) {
            $status.text("Disabled — using TMIVCom Notification.");
            return;
        }

        const permissionText = Notification.permission === "granted"
            ? "Enabled and allowed by the browser."
            : Notification.permission === "denied"
                ? "Enabled, but browser permission is blocked — using TMIVCom Notification."
                : "Enable and save to request browser permission.";
        $status.text(permissionText);
    }

    function applyChromeNotificationPreference(enabled, persist) {
        chromeNotificationsEnabled = enabled === true;
        window.tmivChromeNotificationsEnabled = chromeNotificationsEnabled;
        $("#enableChromeNotifications").prop("checked", chromeNotificationsEnabled);
        if (persist) {
            try {
                localStorage.setItem(
                    getChromeNotificationStorageKey(),
                    String(chromeNotificationsEnabled));
            } catch (error) {
                console.warn("Unable to save Chrome notification preference.", error);
            }
        }
        updateChromeNotificationPermissionStatus();
    }

    function requestChromeNotificationPermissionIfNeeded() {
        if (!chromeNotificationsEnabled
            || !("Notification" in window)
            || Notification.permission !== "default") {
            updateChromeNotificationPermissionStatus();
            return Promise.resolve(window.Notification?.permission ?? "unavailable");
        }

        return Notification.requestPermission()
            .then(function (permission) {
                updateChromeNotificationPermissionStatus();
                return permission;
            })
            .catch(function (error) {
                console.warn("Chrome notification permission request failed.", error);
                updateChromeNotificationPermissionStatus();
                return "denied";
            });
    }

    function setUserSettingsModalVisible(visible) {
        $("#userSettingsModal")
            .prop("hidden", !visible)
            .toggleClass("is-open", visible)
            .attr("aria-hidden", visible ? "false" : "true");
    }

    function renderUserRoleOptions() {
        $("#userRoleOptions").html(userRoleOptions.map(role => `
            <label class="user-role-card ${selectedUserRole === role.code ? "is-selected" : ""}">
                <input type="radio" name="userWorkflowRole" value="${role.code}" ${selectedUserRole === role.code ? "checked" : ""} />
                <span class="user-role-code">${role.code}</span>
                <span class="user-role-description">${role.description}</span>
                <span class="user-role-check"><i class="fa fa-check"></i></span>
            </label>
        `).join(""));
    }

    function refreshUserRoleSettings(openModal = false) {
        return $.get("/api/Employee/GetMyRoleSettings")
            .done(function (response) {
                if (!response?.success) return;
                const profile = response.data;
                currentSettingsAccount = profile.accountName || _loginUser || "";
                savedUserTheme = readThemePreference();
                applyThemePreference(savedUserTheme, false);
                applyChromeNotificationPreference(readChromeNotificationPreference(), false);
                selectedUserRole = (profile.role || "").toUpperCase();
                userRoleOptions = profile.roles?.length ? profile.roles : fallbackRoleOptions;
                $("#accountRoleBadge").text(selectedUserRole || "Role not selected");
                $("#roleLabel").text(selectedUserRole ? `Phòng ban ${selectedUserRole}` : "Phòng ban chưa chọn");
                $("#displayLabel").text(profile.fullName || _displayName || profile.accountName);
                const initial = (profile.fullName || profile.accountName || "U").charAt(0).toUpperCase();
                $("#accountAvatarInitial").text(initial);
                renderUserRoleOptions();
                // Only reveal after the role cards have been fully rendered.
                if (openModal) setUserSettingsModalVisible(true);
            })
            .fail(function (xhr) {
                appNotify({ message: xhr.responseJSON?.message || "Unable to load account settings.", type: "warning" });
            });
    }

    $("#userAccountTrigger").on("click", function (e) {
        e.stopPropagation();
        $("#dropdown-menu").toggle();
        $(this).attr("aria-expanded", $("#dropdown-menu").is(":visible"));
    });

    $(document).on("click", function () {
        $("#dropdown-menu").hide();
        $("#userAccountTrigger").attr("aria-expanded", "false");
    });

    $("#dropdown-menu").on("click", function (e) { e.stopPropagation(); });
    $("#openUserSettings").on("click", function () {
        $("#dropdown-menu").hide();
        refreshUserRoleSettings(true);
    });
    $("#closeUserSettings, #cancelUserSettings, .user-settings-backdrop").on("click", function () {
        applyThemePreference(savedUserTheme, false);
        setUserSettingsModalVisible(false);
    });
    $("#userRoleOptions").on("change", "input[name='userWorkflowRole']", function () {
        selectedUserRole = this.value;
        renderUserRoleOptions();
    });
    $("#enableChromeNotifications").on("change", function () {
        chromeNotificationsEnabled = this.checked === true;
        window.tmivChromeNotificationsEnabled = chromeNotificationsEnabled;
        updateChromeNotificationPermissionStatus();
    });
    $("#accountThemeOptions").on("change", "input[name='accountTheme']", function () {
        applyThemePreference(this.value, false);
    });
    $("#saveUserSettings").on("click", function () {
        if (!selectedUserRole) {
            appNotify({ message: "Please select a role.", type: "warning" });
            return;
        }

        const $button = $(this).prop("disabled", true).addClass("is-saving");
        applyThemePreference(selectedUserTheme, true);
        applyChromeNotificationPreference(
            $("#enableChromeNotifications").prop("checked") === true,
            true);
        requestChromeNotificationPermissionIfNeeded();
        $.ajax({
            url: "/api/Employee/UpdateMyRole",
            method: "POST",
            contentType: "application/json",
            data: JSON.stringify({ role: selectedUserRole })
        }).done(function (response) {
            _role = response.data.role;
            window.syncHomeRoleSwitcher?.(_role);
            window.refreshRouteCommentPermissions?.();
            $("#accountRoleBadge").text(response.data.role);
            $("#roleLabel").text(`Phòng ban ${response.data.role}`);
            setUserSettingsModalVisible(false);
            appNotify({ message: "Role setting saved. Reload the page to refresh menu permissions.", type: "success" });
        }).fail(function (xhr) {
            appNotify({ message: xhr.responseJSON?.message || "Unable to save role setting.", type: "error" });
        }).always(function () {
            $button.prop("disabled", false).removeClass("is-saving");
        });
    });
    refreshUserRoleSettings();


     var url = '@ViewData["HostUrl"]'
            // monitorConnection(url, 'onlineState', 10000);




         // ====== Notification state ======
    const _notif = {
        items: [],
        unreadCount: 0,
        currentFilter: "all",
        reloadRequestId: 0,
        reloadTimer: null,
        openingIds: new Set(),
        pendingReadIds: new Set(),
        // popup: null,
        popover: null,
        list: null,
        typeFilter: null
    };

    const notificationTypeMeta = {
        default: { label: "Default", shortLabel: "DEF", icon: "info", background: window.AppColors.c_f1f5f9, color: window.AppColors.c_475569 },
        assign: { label: "Assign", shortLabel: "ASG", icon: "user", background: window.AppColors.c_ede9fe, color: window.AppColors.c_6d28d9 },
        system: { label: "System", shortLabel: "SYS", icon: "preferences", background: window.AppColors.c_e2e8f0, color: window.AppColors.c_334155 },
        quotation: { label: "Quotation", shortLabel: "QT", icon: "file", background: window.AppColors.c_e0f2fe, color: window.AppColors.c_0369a1 },
        policyissuance: { label: "Policy Issuance", shortLabel: "PI", icon: "shield", background: window.AppColors.c_dcfce7, color: window.AppColors.c_15803d },
        accept: { label: "Accept", shortLabel: "ACC", icon: "check", background: window.AppColors.c_ccfbf1, color: window.AppColors.c_0f766e },
        success: { label: "Success", shortLabel: "OK", icon: "check", background: window.AppColors.c_dcfce7, color: window.AppColors.c_166534 },
        fail: { label: "Fail", shortLabel: "FAIL", icon: "close", background: window.AppColors.c_fee2e2, color: window.AppColors.c_b91c1c },
        initial: { label: "Initial", shortLabel: "INI", icon: "add", background: window.AppColors.c_dbeafe, color: window.AppColors.c_1d4ed8 },
        reminder: { label: "Reminder", shortLabel: "REM", icon: "clock", background: window.AppColors.c_fef3c7, color: window.AppColors.c_b45309 },
        alert: { label: "Alert", shortLabel: "ALT", icon: "warning", background: window.AppColors.c_ffedd5, color: window.AppColors.c_c2410c },
        comment: { label: "Comment", shortLabel: "CMT", icon: "warning", background: window.AppColors.c_ffedd5, color: window.AppColors.c_c2410c }
    };

    const notificationTypeKeys = [
        "default", "assign", "system", "policyissuance", "quotation", "accept",
        "success", "fail", "initial", "reminder", "alert", "comment"
    ];

    function normalizeNotificationType(value) {
        return String(value || "").trim().toLowerCase().replace(/[\s_-]+/g, "");
    }

    function getNotificationTypeEnumItem(typeKey) {
        const aliases = {
            default: ["default"],
            assign: ["assign", "assignment"],
            system: ["system", "sys"],
            quotation: ["quotation", viewIdModulePrefix.quotation],
            policyissuance: ["policyissuance", "policy", viewIdModulePrefix.policyIssuance],
            accept: ["accept", "accepted"],
            success: ["success", "successful"],
            fail: ["fail", "failed", "failure"],
            initial: ["initial", "initialize", "initialized", "initiate", "initiated", "initiation", "inititial"],
            reminder: ["reminder", "remind"],
            alert: ["alert"],
            comment: ["comment"]
        };
        const acceptedValues = aliases[typeKey] || [typeKey];

        return (_enums.NotificationType || []).find(enumItem => {
            const values = [enumItem?.code, enumItem?.key, enumItem?.value, enumItem?.name, enumItem?.text]
                .map(normalizeNotificationType);
            return values.some(value => acceptedValues.includes(value));
        }) || null;
    }

    function getNotificationTypeEnumId(typeKey) {
        const enumItem = getNotificationTypeEnumItem(typeKey);
        return enumItem?.id ?? enumItem?.Id ?? null;
    }

    function getNotificationTypeEnumText(enumItem) {
        return enumItem?.text ?? enumItem?.Text
            ?? enumItem?.value ?? enumItem?.Value
            ?? enumItem?.code ?? enumItem?.Code
            ?? enumItem?.key ?? enumItem?.Key
            ?? enumItem?.name ?? enumItem?.Name
            ?? "";
    }

    function buildNotificationTypeFilterDataSource() {
        const result = [
            { value: "all", text: "All categories", typeId: null },
            { value: "unread", text: "Unread", typeId: null }
        ];
        const seenIds = new Set();

        [...(_enums.NotificationType || [])]
            .sort((left, right) =>
                Number(left?.enumOrder ?? left?.EnumOrder ?? 0)
                - Number(right?.enumOrder ?? right?.EnumOrder ?? 0)
            )
            .forEach(enumItem => {
                const typeId = enumItem?.id ?? enumItem?.Id;
                if (typeId === null || typeId === undefined || seenIds.has(String(typeId))) return;

                seenIds.add(String(typeId));
                result.push({
                    value: `type:${typeId}`,
                    text: getNotificationTypeEnumText(enumItem) || `Type ${typeId}`,
                    typeId: typeId
                });
            });

        return result;
    }

    function refreshNotificationTypeFilterData() {
        if (!_notif.typeFilter) return;

        const dataSource = buildNotificationTypeFilterDataSource();
        const currentValue = _notif.currentFilter || "all";
        const nextValue = dataSource.some(item => item.value === currentValue) ? currentValue : "all";

        _notif.currentFilter = nextValue;
        _notif.typeFilter.option({
            dataSource: dataSource,
            value: nextValue
        });
    }

    function getNotificationItemTypeId(item) {
        let enumType = item?.type ?? item?.Type;
        if (typeof enumType === "string" && enumType.trim().startsWith("{")) {
            try { enumType = JSON.parse(enumType); } catch { }
        }

        if (enumType && typeof enumType === "object") {
            return enumType.id ?? enumType.Id ?? item?.typeId ?? item?.TypeId ?? null;
        }

        if (typeof enumType === "number" || /^\d+$/.test(String(enumType || ""))) {
            return enumType;
        }
        return item?.typeId ?? item?.TypeId ?? null;
    }

    function getNotificationType(item) {
        const notificationTypeId = getNotificationItemTypeId(item);
        if (notificationTypeId !== null && notificationTypeId !== undefined) {
            for (const typeKey of notificationTypeKeys) {
                const enumId = getNotificationTypeEnumId(typeKey);
                if (enumId !== null && String(enumId) === String(notificationTypeId)) {
                    // Old notifications can have the Default type even though their
                    // title clearly identifies a more specific event. Keep evaluating
                    // those items so the title fallback below can classify them.
                    if (typeKey !== "default") return typeKey;
                    break;
                }
            }
        }

        let enumType = item?.type ?? item?.Type;
        if (typeof enumType === "string" && enumType.trim().startsWith("{")) {
            try { enumType = JSON.parse(enumType); } catch { }
        }

        const candidates = typeof enumType === "object" && enumType
            ? [enumType.code, enumType.key, enumType.value, enumType.name, enumType.text, enumType.displayName]
            : [enumType, item?.typeCode, item?.typeName];

        for (const candidate of candidates) {
            const type = normalizeNotificationType(candidate);
            if (type === "default") continue;
            if (type.includes("assign")) return "assign";
            if (type === "system" || type === "sys") return "system";
            if (type === viewIdModulePrefix.quotation || type.includes("quotation")) return "quotation";
            if (type === viewIdModulePrefix.policyIssuance || type.includes("policyissuance") || type.includes("policy")) return "policyissuance";
            if (type.includes("accept")) return "accept";
            if (type.includes("success")) return "success";
            if (type.includes("fail")) return "fail";
            if (type.includes("initial")) return "initial";
            if (type.includes("remind")) return "reminder";
            if (type.includes("alert")) return "alert";
            if (type.includes("comment")) return "comment";
        }

        // Backward compatibility for notifications created before Type was populated.
        const title = String(item?.title ?? item?.Title ?? "").toUpperCase();
        const normalizedTitle = normalizeNotificationType(title);
        if (
            normalizedTitle.includes("initial")
            || normalizedTitle.includes("inititial")
            || normalizedTitle.includes("initiat")
            || /\b(?:CREATE|CREATED|START|STARTED)\b/.test(title)
        ) return "initial";
        if (title.includes("[QT]")) return "quotation";
        if (title.includes("[PI]")) return "policyissuance";
        return "default";
    }

    // updateNotification(100);



    function setNotifCount(n) {
        n = Math.max(0, Number(n) || 0);
        const $count = $("#notificationBtn .notif-count");
        if (!n || n <= 0) {
            $count.text("").hide();
            hideNotificationDot();
        } else {
            $count.text(n > 99 ? "99+" : n).show();
            showNotificationDot();
        }
    }

    // ====== API call (mock) ======


    function apiMarkRead(id) {
        const notificationId = String(id ?? "");
        _notif.pendingReadIds.add(notificationId);
        const request = ajaxPut('/api/Notification/UpdateData', {
            key: id,
            values: JSON.stringify({ isRead: true })
        });
        request.always(function () {
            _notif.pendingReadIds.delete(notificationId);
        });
        return request;
    }

    async function apiMarkAllRead() {
        const unreadItems = (_notif.items || []).filter(item => !item.isRead);
        const results = await Promise.allSettled(unreadItems.map(item => apiMarkRead(item.id)));
        let failed = 0;
        results.forEach((result, index) => {
            if (result.status === "fulfilled") {
                unreadItems[index].isRead = true;
            } else {
                failed += 1;
            }
        });
        return { updated: unreadItems.length - failed, failed };
    }

    function ensureNotificationPopup() {
        if (_notif.popover) return;

        _notif.popover = $("#notificationPopOver").dxPopover({
            target: "#notificationBtn",       // 🔔 neo theo bell
            position: {
                my: "right top",
                at: "right bottom",
                offset: "0 8"
            },
            width: 420,
            height: 520,
            shading: false,                   // ❌ KHÔNG overlay
            closeOnOutsideClick: true,
            showCloseButton: false,
            wrapperAttr: {
                class: "notif-popover-wrapper"
            },
            contentTemplate: function (contentEl) {
               const $toolbar = $(`
                    <div style="padding:5px;display:flex;gap:8px;align-items:center;margin-bottom:5px;">
                      <div id="notifMarkAllBtn"></div>
                      <div id="notifCategorySelect" style="flex:1;min-width:0;"></div>
                      <div style="margin-left:auto; opacity:.7; font-size:12px;" id="notifHint"></div>
                    </div>
                    <div id="notifList" style="padding: 5px"></div>
                `);

                $(contentEl).append($toolbar);

                $("#notifMarkAllBtn").dxButton({
                    text: "Mark all as read",
                    icon: "check",
                    type: "default",
                    stylingMode: "outlined",
                    onClick: async function () {
                        const result = await apiMarkAllRead();
                        refreshNotifUI();
                        if (result.failed) {
                            DevExpress.ui.notify(`${result.failed} notification(s) could not be updated`, "warning", 1800);
                        }
                    }
                });

                _notif.typeFilter = $("#notifCategorySelect").dxSelectBox({
                    dataSource: buildNotificationTypeFilterDataSource(),
                    valueExpr: "value",
                    displayExpr: "text",
                    value: _notif.currentFilter || "all",
                    width: "100%",
                    height: 36,
                    searchEnabled: true,
                    searchExpr: ["text"],
                    placeholder: "Notification category",
                    onValueChanged: function (e) {
                        _notif.currentFilter = e.value || "all";
                         refreshNotifUI();
                    }
                }).dxSelectBox("instance");

                _notif.list = $("#notifList").dxList({
                    dataSource: _notif.items,
                    height: 400,
                    noDataText: "No notifications",
                    searchEnabled: true,
                    searchExpr: ["title", "message"],
                    itemTemplate: function (itemData) {
                        // item UI
                        const isUnread = !itemData.isRead;
                        const timeText = formatTime(itemData.createdDate);
                        const notificationType = getNotificationType(itemData);
                        const typeMeta = notificationTypeMeta[notificationType] || notificationTypeMeta.default;

                        let title = itemData.title || "";
                        // Match bracket prefix at start like [QT] or [PI]
                        const match = title.match(/^\[([^\]]+)\]/);
                        if (match) {
                            title = title.substring(match[0].length).trim(); // Clean title
                        }

                        const typeBadge = `<span title="${typeMeta.label}" style="padding:2px 6px;border-radius:6px;font-size:10px;font-weight:700;background:${typeMeta.background};color:${typeMeta.color};margin-right:6px;text-transform:uppercase;">${typeMeta.shortLabel}</span>`;

                        const $notificationItem = $(`
                            <div class="notif-list-item" style="padding:5px 8px; border-bottom:1px solid var(--c-rgba-0-0-0-06);">
                              <div style="display:flex; gap:8px; align-items:flex-start;">
                                <div style="width:10px; margin-top:6px;">
                                  <span style="display:${isUnread ? "inline-block" : "none"}; width:8px; height:8px; border-radius:50%; background:var(--c-ff3b30);"></span>
                                </div>
                                <div style="flex:1;">
                                  <div class="notif-item-heading">
                                    <div class="notif-item-title" style="font-weight:${isUnread ? 700 : 600};">
                                      ${typeBadge}
                                      <span class="notif-item-title-text">${escapeHtml(title)}</span>
                                    </div>
                                    <div class="notif-item-time">${timeText}</div>
                                  </div>
                                  <div class="notif-message-preview rich-html-preview">
                                    ${itemData.message || ""}
                                  </div>
                                  <div style="margin-top:8px; display:flex; gap:8px;">
                                    <a href="javascript:void(0)" class="notif-mark">${isUnread ? "Mark read" : "Read"}</a>
                                    <a href="javascript:void(0)" class="notif-view">Click to view more</a>
                                  </div>
                                </div>
                              </div>
                            </div>
                        `)
                        .css("cursor", "pointer")
                        .data("item", itemData)
                        .on("click", function (e) {
                            // Links own their actions; every other area opens the notification record.
                            if ($(e.target).closest("a").length) return;
                            openNotification(itemData);
                        })
                        .on("click", ".notif-mark", async function (e) {
                            e.stopPropagation();
                            if (!itemData.isRead) {
                                try {
                                    await apiMarkRead(itemData.id);
                                    itemData.isRead = true;
                                    refreshNotifUI();
                                } catch (err) {
                                    DevExpress.ui.notify("Unable to mark notification as read", "warning", 1500);
                                }
                            }
                        }).on("click", ".notif-view", async function (e) {
                            e.stopPropagation();
                            openMessageDialog(itemData);
                            // mở popup đọc nội dung, không ảnh hưởng open/mark read
                        });
                        setTimeout(function () { updateRichPreviewOverflow($notificationItem); }, 0);
                        setTimeout(function () { updateRichPreviewOverflow($notificationItem); }, 350);
                        return $notificationItem;
                    }
                }).dxList("instance");
            }
        }).dxPopover("instance");
    }

    function formatTime(dt) {
           try {
               const d = (dt instanceof Date) ? dt : new Date(dt);
               if (Number.isNaN(d.getTime())) return "";
               return d.toLocaleTimeString("en-US", {
                   hour: "numeric",
                   minute: "2-digit"
               });
           } catch { return ""; }
       }



    // ====== open item ======
    function getNotificationTarget(item) {
        let target = item?.url ?? item?.Url ?? "";
        for (let i = 0; i < 3 && typeof target === "string"; i++) {
            const value = target.trim();
            if (!value) return null;
            if (!(value.startsWith("{") || value.startsWith('"'))) {
                target = { url: value };
                break;
            }
            try { target = JSON.parse(value); } catch { return null; }
        }

        if (target?.url && typeof target.url === "object") target = target.url;
        if (!target || typeof target !== "object") return null;

        const url = target.url ?? target.Url;
        if (!url || typeof url !== "string") return null;
        return {
            url: url,
            caption: target.caption ?? target.Caption ?? target.code ?? target.Code
                ?? `notification_${item?.recordGuid ?? item?.RecordGuid ?? item?.id ?? item?.Id}`,
            name: target.name ?? target.Name ?? item?.title ?? item?.Title ?? "Notification",
            data: target.data ?? target.Data ?? null
        };
    }

    async function openNotification(item) {
        const itemId = String(item?.id ?? item?.Id ?? "");
        if (_notif.openingIds.has(itemId)) return;
        _notif.openingIds.add(itemId);
        setTimeout(() => _notif.openingIds.delete(itemId), 800);

        // 1) mark read
        if (!item.isRead) {
            item.isRead = true;
            refreshNotifUI();
            apiMarkRead(item.id).catch(function (err) {
                item.isRead = false;
                refreshNotifUI();
                console.warn("Unable to mark notification as read", err);
            });
        }

        // 2) hành động mở: tùy hệ bạn
        // Nếu bạn có obj.url/caption/name kiểu loadParams -> callElementView(...)
        const target = getNotificationTarget(item);
        if (!target) {
            DevExpress.ui.notify("No target action for this notification", "warning", 1500);
            return;
        }

        _notif.popover?.hide();
        callElementView(target.url, target.caption, target.name, target.data);
    }
    function refreshNotifUI() {
        _notif.unreadCount = (_notif.items || []).filter(x => x.receivedBy == _loginUser && !x.isRead).length;
        setNotifCount(_notif.unreadCount);

        if (_notif.list) {
            let filteredItems = _notif.items || [];
            const filter = _notif.currentFilter || "all";
            if (filter === "unread") {
                filteredItems = filteredItems.filter(x => !x.isRead);
            } else if (filter.startsWith("type:")) {
                const filterTypeId = filter.substring("type:".length);
                filteredItems = filteredItems.filter(x =>
                    String(getNotificationItemTypeId(x)) === String(filterTypeId)
                );
            } else if (notificationTypeKeys.includes(filter)) {
                const filterTypeId = getNotificationTypeEnumId(filter);
                filteredItems = filteredItems.filter(x => {
                    const itemTypeId = getNotificationItemTypeId(x);
                    if (filterTypeId !== null && itemTypeId !== null) {
                        return String(itemTypeId) === String(filterTypeId);
                    }
                    return getNotificationType(x) === filter;
                });
            }
            _notif.list.option("dataSource", filteredItems);
            $("#notifHint").text(_notif.unreadCount ? `${_notif.unreadCount} unread` : "");
        }
    }

    // ====== public function you already call ======
    function updateNotification() {
        clearTimeout(_notif.reloadTimer);
        _notif.reloadTimer = setTimeout(function () {
            reloadNotifications().catch(function () { });
        }, 80);
    }

    function invokePageTab(objString, isNotify = false){
        var  obj = JSON.parse(objString);
                  setTimeout(() => {
                    callElementView(obj.url, obj.caption, obj.name, null)
                    if (!isNotify)
                    window.history.replaceState(null, '', '/Management#');
                   }, 1000);
    }

    ensureNotificationPopup();
     updateNotification();
     $("#notificationBtn").off("click.notification").on("click.notification", function () {
         $(this).toggleClass("is-open");
     // _notif.popup.show();
     _notif.popover.toggle();



     });




     document.addEventListener("DOMContentLoaded", function () {
        const pushMenuBtn = document.querySelector('[data-toggle="push-menu"]');
        if (!pushMenuBtn) return;
        const syncLoadedFormLayout = function () {
            // DevExtreme measures forms and grids at render time. Recalculate
            // once immediately and once after the 300ms sidebar transition.
            window.dispatchEvent(new Event("resize"));
        };
        pushMenuBtn.addEventListener("click", function () {
            window.requestAnimationFrame(syncLoadedFormLayout);
            window.setTimeout(syncLoadedFormLayout, 340);
            setTimeout(function () {
                const body = document.body;

                // if (body.classList.contains("sidebar-collapse")) {
                //     // sidebar collapsed → reset margin-right
                //     document.documentElement.style.setProperty("--collapsed-mr", "-22%");
                //     document.documentElement.style.setProperty("--collapsed-form-mr", "-42%");
                //     // document.documentElement.style.setProperty("--collapsed-grid-max-mr", "70%");
                // } else {
                //     document.documentElement.style.setProperty("--collapsed-mr", "-22%");
                //     document.documentElement.style.setProperty("--collapsed-form-mr", "-26%");
                //     // document.documentElement.style.setProperty("--collapsed-grid-max-mr", "73%");
                // }
            }, 0);
        });
    });

    function syncNewRequestButtonVisibility(department) {
        const newRequestDepartment = String(qtDeptModuleCode[0] || "").trim().toUpperCase();
        const userDepartment = String(department || "").trim().toUpperCase();
        const canCreateRequest = newRequestDepartment !== "" && (userDepartment === "MKT" || userDepartment === "IT");
        $("#btnNew").toggle(canCreateRequest).attr("aria-hidden", String(!canCreateRequest));
    }
    window.syncNewRequestButtonVisibility = syncNewRequestButtonVisibility;

    let moduleLauncherPopup;
    function openModuleLauncherPopup() {
       if (!moduleLauncherPopup) {
           var moduleLauncherPopup = makePopupWithScroll(
                $("#moduleLauncherPopup"),
                "small",
                ""
            );



           moduleLauncherPopup = $("#moduleLauncherPopup").dxPopup({
               title: "Create New",
               // width: "52vw",
               // height: "52vh",
               // minWidth: 760,
               // minHeight: 420,
               // maxWidth: 1100,
               // maxHeight: 700,
               // width: function () { return Math.min(1200, window.innerWidth * 0.92); },
               // height: function () { return Math.min(760, window.innerHeight * 0.9); },
               // minWidth: Math.min(320, window.innerWidth * 0.92),
               // minHeight: Math.min(320, window.innerHeight * 0.9),
               showTitle: false,
               showCloseButton: true,
               closeOnOutsideClick: true,
               dragEnabled: false,
               wrapperAttr: { class: "module-launcher-popup" },
               contentTemplate: function (container) {
                   const html = `
    <div class="launcher-wrap">

        <div class="launcher-title">Select action</div>

        <div class="launcher-grid">


            <div class="action-card quotation" id="btnQuotation">

                <div class="card-image">
                    <img src="img/Man_Quotation.png">
                </div>

                <div class="card-body">

                    <div class="card-icon">
                        📄
                    </div>

                    <div class="card-title">
                        Quotation Request
                    </div>

                    <div class="card-desc">
                        Prepare and submit a new quotation request
                    </div>


                </div>


            </div>
                    <div class="action-card quotation" id="btnQuotationApproval">

                        <div class="card-image">
                            <img src="img/Quotation_Approve.png">
                        </div>

                        <div class="card-body">

                            <div class="card-icon">
                                    📝
                            </div>

                            <div class="card-title">
                                Quotation Approval Request
                            </div>

                            <div class="card-desc">
                                Prepare and submit a new quotation request
                            </div>

                        </div>

            </div>

            <div class="action-card policy" id="btnPolicy">

                <div class="card-image">
                        <img src="img/PolicyIssuance_Client.png">
                </div>


                <div class="card-body">

                    <div class="card-icon policy-icon">

    <svg
    width="45"
    height="45"
    viewBox="0 0 64 64">

    <rect
    x="12"
    y="5"
    width="40"
    height="52"
    rx="5"
    fill=window.AppColors.c_5dade2/>


    <path
    d="M20 18H44M20 28H44M20 38H44"
    stroke="white"
    stroke-width="4"
    stroke-linecap="round"/>


    <circle
    cx="48"
    cy="46"
    r="10"
    fill=window.AppColors.c_f5b041/>


    <path
    d="M44 46l3 3 6-7"
    stroke="white"
    stroke-width="3"
    fill="none"/>

    </svg>
                    </div>

                    <div class="card-title">
                        Policy Issuance Request
                    </div>


                    <div class="card-desc">
                        Submit a request for policy issuance
                    </div>

                </div>

            </div>

            <div class="action-card policy" id="btnPolicyEndorse">

                <div class="card-image">
                    <img src="img/PolicyIssuance_Client.png">
                </div>

                <div class="card-body">
                    <div class="card-icon policy-icon">&#9998;</div>

                    <div class="card-title">
                        Policy Issuance - Endorse
                    </div>

                    <div class="card-desc">
                        Create a direct endorsement request and start at FO
                    </div>
                </div>

            </div>


        </div>

    </div>`;



        // <div class="launcher-item survey" id="btnQuotationRequest">
    // <div class="icon">📝</div>
    // <div class="launcher-text">Create Quotation </div>
    // <div  class="launcher-text">(TS)</div>
    // </div>
               $(container).append(html);
               $("#btnQuotation").on("click", function () {
                   newQuotationForm();
                   moduleLauncherPopup.hide();
               });
                    $("#btnQuotationApproval").on("click", function () {
                   newQuotationApprovalForm();
                   moduleLauncherPopup.hide();
               });
               $("#btnPolicy").on("click", function () {
                   newPolicyIssuanceForm();
                   moduleLauncherPopup.hide();
               });
               $("#btnPolicyEndorse").on("click", function () {
                   newPolicyIssuanceEndorsementForm();
                   moduleLauncherPopup.hide();
               });
           }
           }).dxPopup("instance");
       }
       moduleLauncherPopup.show();
    }


        function handleFloating() {
        var height = $(".main-header.wrapper").height();
        const $activeLi = $('li.active');
        const $el = $('li.active').prevObject.find('[aria-controls]');

        if (!$el.length) return;
        var count = $('[aria-controls]');
        // reset trước để tránh bị dính nhiều element
        $('[aria-controls]').css({
            position: '',
            opacity: '',
            left: '',
            zIndex: ''
        });

        // if ($(window).scrollTop() > height) {  // selectbox collapse problem
        //     $el.css({
        //         position: "fixed",
        //         zIndex: "99999",
        //         left: "43%",
        //         transition: "opacity 0.5s ease",
        //         opacity: 1
        //     });
        // }

        // if ((responseData.surveyData.progressvalue == 100) && ($el.css("position") === "fixed")) {
        //     setTimeout(() => {
        //         $el.css({ opacity: 0 });
        //     }, 500);

        //     setTimeout(() => {
        //         $el.css({
        //             position: "inherit",
        //             left: "43%",
        //             zIndex: "100",
        //             opacity: 1,
        //         });
        //     }, 1000);
        // }
    }


     // $("#btnNew").dxButton({
     //    text: "New request",
     //    icon: "plus",
     //    // elementAttr: {
     //    //     class: "plus-icon-btn"
     //    // },
     //    onClick: () => openModuleLauncherPopup()
     // });
      // ================= MODULE POPUP =================


          $("#btnNew").dxButton({
       type: "normal",
       stylingMode: "contained",
       template: function (data, container) {
           $(`
    <div>
    <svg
                        width="18"
                        height="18"
                        viewBox="0 0 512 512"
                        xmlns="http://www.w3.org/2000/svg">
    <circle
                           cx="256"
                           cy="256"
                           r="256"
                           fill=var(--theme-primary) />
    <path
                           d="M256 140V372M140 256H372"
                           stroke="white"
                           stroke-width="36"
                           stroke-linecap="round" />
    </svg>
    <span style="font-size: 11px;">
                        New request
    </span>
    </div>
           `).appendTo(container);
       },
       onClick: () => openModuleLauncherPopup()
    });
    syncNewRequestButtonVisibility(_userDepartment);

    $(window).on('scroll', function () {
        handleFloating();
    });


</script>
<style>
       
</style>
