@model OverviewScrollPartialModel

@{
    string Id(string name) => $"{Model.Prefix}-{name}_{Model.Id}";
    string Cd(string name) => $"{Model.Prefix}-{name}";
    var moduleCode = (Model.ModuleCode ?? "").Trim().ToLower();
    string[] qtDeptModuleCode = new string[] { "FO", "TS", "UW", "LMKT", "PM" };
    string[] piDeptModuleCode = new string[] { "FO", "TS", "PM" };
}

<div id="@Id("overviewScroll")" class="overview-scroll">

    @if (moduleCode == "qt")
    {
        @foreach (string item in qtDeptModuleCode)
        {
            <section id="@Id($"sec{item}_{Model.Id}")" class="dept-section">
                <div id="@($"{Model.Id}")" class="dept-header">
                    @await Html.PartialAsync($"Business/Form/QuotationDetail/_Quotation_{item}_Form_Header", null, new ViewDataDictionary(ViewData) { ["Id"] = ($"{Model.Id}"), ["Cd"] = Cd($"sec{item}-header") })
                </div>
                <div id="@Id($"sec{item}-upperbody_{Model.Id}")" class="dept-upperbody">
                    @await Html.PartialAsync($"Business/Form/QuotationDetail/_Quotation_{item}_Form_UpperBody", null, new ViewDataDictionary(ViewData) { ["Id"] = ($"{Model.Id}"), ["Cd"] = Cd($"sec{item}-upperbody") })
                </div>
                <div id="@Id($"sec{item}-body_{Model.Id}")" class="dept-body">
                    @await Html.PartialAsync($"Business/Form/QuotationDetail/_Quotation_{item}_Form", null, new ViewDataDictionary(ViewData) { ["Id"] = ($"{Model.Id}"), ["Cd"] = Cd($"sec{item}-body") })
                </div>
                <div id="@Id($"sec{item}-lowerbody_{Model.Id}")" class="dept-lowerbody">
                    @await Html.PartialAsync($"Business/Form/QuotationDetail/_Quotation_{item}_Form_LowerBody", null, new ViewDataDictionary(ViewData) { ["Id"] = ($"{Model.Id}"), ["Cd"] = Cd($"sec{item}-lowerbody") })
                </div>
                <div id="@Id($"sec{item}-footer_{Model.Id}")" class="dept-footer">
                    @await Html.PartialAsync($"Business/Form/QuotationDetail/_Quotation_{item}_Form_Footer", null, new ViewDataDictionary(ViewData) { ["Id"] = ($"{Model.Id}"), ["Cd"] = Cd($"sec{item}-footer") })
                </div>
            </section>
        }
        
    }
    else if (moduleCode == "pi")
    {
        @foreach (string item in piDeptModuleCode)
        {
            <section id="@Id($"sec{item}_{Model.Id}")" class="dept-section">
                <div id="@($"{Model.Id}")" class="dept-header">
                    @await Html.PartialAsync($"Business/Form/PolicyIssuanceDetail/_PolicyIssuance_{item}_Form_Header", null, new ViewDataDictionary(ViewData) { ["Id"] = ($"{Model.Id}"), ["Cd"] = Cd($"sec{item}-header") })
                </div>
                <div id="@Id($"sec{item}-upperbody_{Model.Id}")" class="dept-upperbody">
                    @await Html.PartialAsync($"Business/Form/PolicyIssuanceDetail/_PolicyIssuance_{item}_Form_UpperBody", null, new ViewDataDictionary(ViewData) { ["Id"] = ($"{Model.Id}"), ["Cd"] = Cd($"sec{item}-upperbody") })
                </div>
                <div id="@Id($"sec{item}-body_{Model.Id}")" class="dept-body">
                    @await Html.PartialAsync($"Business/Form/PolicyIssuanceDetail/_PolicyIssuance_{item}_Form", null, new ViewDataDictionary(ViewData) { ["Id"] = ($"{Model.Id}"), ["Cd"] = Cd($"sec{item}-body") })
                </div>
                <div id="@Id($"sec{item}-lowerbody_{Model.Id}")" class="dept-lowerbody">
                    @await Html.PartialAsync($"Business/Form/PolicyIssuanceDetail/_PolicyIssuance_{item}_Form_LowerBody", null, new ViewDataDictionary(ViewData) { ["Id"] = ($"{Model.Id}"), ["Cd"] = Cd($"sec{item}-lowerbody") })
                </div>
                <div id="@Id($"sec{item}-footer_{Model.Id}")" class="dept-footer">
                    @await Html.PartialAsync($"Business/Form/PolicyIssuanceDetail/_PolicyIssuance_{item}_Form_Footer", null, new ViewDataDictionary(ViewData) { ["Id"] = ($"{Model.Id}"), ["Cd"] = Cd($"sec{item}-footer") })
                </div>
            </section>
        }
    }

</div>