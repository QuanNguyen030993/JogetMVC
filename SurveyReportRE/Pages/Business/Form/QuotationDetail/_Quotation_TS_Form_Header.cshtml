@using JogetMVC.Model
@{
    string dept = "TS";
    long quotationId = Convert.ToInt64(ViewData["Id"]);
}

<div class="dept-title">@dept</div>
<div class="dept-form-header">
    <div class="dept-form-header__assign">
        <div id="@QuotationIdHelper.AssigneeBox(dept, quotationId)"></div>
    </div>
    <div id="@QuotationIdHelper.BtnToggleResForm"></div>
</div>
<div id="@QuotationIdHelper.StickPanel(dept, quotationId)">
    @* <div class="sticky-wrap wobble" data-tooltip-toggle>
    <div class="pin">
    </div>
    <div class="sticky-note"></div>
    <div class="sticky-badge">1</div>
    <div class="sticky-mini-text">FROM TS</div>
    <div class="note-tooltip">
        <strong>Suggested use</strong><br>
        Icon này có thể gắn ở header section. Khi section trước có comment hoặc giao việc, chỉ cần bật icon sticky note để tạo điểm nhấn trực quan.
    </div>
</div> *@
</div>
<div class="dept-hint-wrap">
    <div class="dept-hint" id="@QuotationIdHelper.Hint(dept, quotationId)"></div>
    <div class="dept-updated" id="@QuotationIdHelper.Upd(dept, quotationId)" title="Last updated">—</div>
    <div id="@QuotationIdHelper.ExpandCollapsedBtn(dept, quotationId)" class="dept-toggle-icon" aria-hidden="true">▾</div>
</div>


@* <div class="dept-title">TS</div>
<div class="dept-form-header">
    <div class="dept-form-header__assign">
        <div id="qt-tsAssigneeBox"></div>
    </div>
</div>
<div class="dept-hint-wrap">
    <div class="dept-hint" id="qt-hintTS"></div>
    <div class="dept-updated" id="qt-updTS" title="Last updated">—</div>
    <div class="dept-toggle-icon" aria-hidden="true">▾</div>
</div> *@
<script>
    setTimeout( () => {
    var formItems = $("#qt-formTS").dxForm("instance")?.option("formData") || {};
    // renderDepartmentAssigneeBox("#qt-tsAssigneeBox", {
    //     groupName: "TS",
    //     dataForm: formItems,
    //     value: formItems.TSAssigneeId || null,
    //     onChanged: function (item, editor) {
    //     }
    // });
    }, _delayRenderAssigneeBox);

</script>