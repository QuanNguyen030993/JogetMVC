@using JogetMVC.Model
@{
    string dept = "TS";
    long quotationId = Convert.ToInt64(ViewData["Id"]); string quotationGuid = ViewData["Guid"].ToString();
}
<style>
    .dx-tabpanel .dx-tabpanel-tabs .dx-tab {
        width: auto;
        padding-left: 18px;
        padding-right: 18px;
    }
</style>
<script type="text/javascript">
    var that = new Object();
    that.ModelName = "Quotation";
    $(`#@QTViewIdHelper.Form(dept, quotationId)`).dxForm({
    dataSource: makeBasicDataSource(that, true),
     items: [
         {itemType: "group",caption: "Decision",colCount:2,items: [
    {
        dataField: "decision",
        label: { text: "Decision" },
        editorType: "dxRadioGroup",
        editorOptions: {
            items: [
                { id: "AC_@QTViewIdHelper.Decision(dept, quotationId)" , text: "Accept", value: true },
                { id: "AS_@QTViewIdHelper.Decision(dept, quotationId)" , text: "Assign", value: false },
                { id: "RJ_@QTViewIdHelper.Decision(dept, quotationId)" , text: "Reject", value: false }
            ],
             displayExpr: "text",
             valueExpr: "id",
             layout: "horizontal",
             itemTemplate: function(itemData, _, itemElement) {
             setTimeout(() => {
              var formItems = $(`#@QTViewIdHelper.Form(dept, quotationId)`).dxForm("instance")?.option("formData") || {};
              var tatObject = JSON.parse(formItems.turnAroundTimeAttributes);
              if (tatObject[`@dept`].AcceptDate)
              {
                $(`#AC_@QTViewIdHelper.Decision(dept, quotationId)`).addClass("btn-info");
                $(`#${itemData.id}`).dxButton("instance").option("disabled", true);
                if (itemData.id === "RJ_@QTViewIdHelper.Decision(dept, quotationId)" ) $(`#RJ_@QTViewIdHelper.Decision(dept, quotationId)`).dxButton("instance").option("disabled", false);
              }
             },_delayRenderDecisionButton);

             $(`<div id='${itemData.id}'>`).dxButton({
             text: itemData.text,
             width: "100%",
             height: "100%",
             onClick: function (e){
                 if (_ == 0)
                 {
                      var formItems = $(`#@QTViewIdHelper.Form(dept, quotationId)`).dxForm("instance")?.option("formData") || {};
                       var tatObject = JSON.parse(formItems.turnAroundTimeAttributes);
                      tatObject[`@dept`].AcceptDate = formatDateToString(new Date()).gmt7;
                      formItems.turnAroundTimeAttributes = JSON.stringify(tatObject);
                      var passingObject = new Object(); 
                      passingObject.statusName = "@dept Process"; // base on Dept
                      passingObject.Comment = `Quotation ${formItems.quotationCode} accepted by ${_displayName}!`;  // base on Dept
                      passingObject.formItems = formItems; 
                      passingObject.dept = `@dept`;
                      passingObject.url = '/api/Quotation/UpdateData';
                      passingObject.urlLog = `/api/Quotation/LogAction`;
                      passingObject.schemeName = `Quotation`;
                      acceptDecision(passingObject);
                 }
                 if (_ == 2)
                 {
                      var formItems = $(`#@QTViewIdHelper.Form(dept, quotationId)`).dxForm("instance")?.option("formData") || {};
                      var tatObject = JSON.parse(formItems.turnAroundTimeAttributes);
                      tatObject[`@dept`].AcceptDate = "";
                      formItems.turnAroundTimeAttributes = JSON.stringify(tatObject);
                      var passingObject = new Object();
                      passingObject.statusName = "@dept Pending"; // base on Dept
                      passingObject.Comment = `Quotation ${formItems.quotationCode} rejected by ${_displayName}!`;  // base on Dept
                      passingObject.formItems = formItems;
                      passingObject.dept = `@dept`;
                      passingObject.url = '/api/Quotation/UpdateData';
                      passingObject.urlLog = `/api/Quotation/LogAction`;
                      passingObject.schemeName = `Quotation`;
                      rejectDecision(passingObject);
                 }
             }

             }).appendTo(itemElement);
          itemElement
        },
        isRequired: true
        }
    }]},

    { itemType: "group", caption: "Attachment", colCount: 1, items: attachmentDefault("qt", "@(dept)", @(quotationId), "@(quotationGuid)") },
    {dataField: "biQtNum",editorType: "dxSelectBox",visible:true},
    {dataField: "incBIQuoId",editorType: "dxTextBox",visible:true},
    {dataField: "iarQuotationNo",editorType: "dxTextBox",visible:true},
    {dataField: "IncIARQuoId",editorType: "dxSelectBox",visible:true},
    {dataField: "tmiCode",editorType: "dxSelectBox",visible: true},

    {dataField: "tmivShare",editorType: "dxNumberBox",visible:true},

        { dataField: "currencyId", label: { template: labelTpl("CurrencyCode - Currency code") } },

            {itemType: "group",caption: "Insurance Period",colCount:2,items: [
    {dataField: "insPeriodId",editorType: "dxSelectBox",visible: true},
    {dataField: "periodInsuranceStartDate",editorType: "dxTextBox",visible:true},
    {dataField: "periodInsuranceEndDate",editorType: "dxTextBox",visible:true},
    {dataField: "actualDays",editorType: "dxNumberBox",visible:true}
    ]},
    {itemType: "group",caption: "Coverage",colCount:2,items: [
        {dataField: "coverageId",editorType:  "dxTextBox",visible:true} // "dxDataGrid"
    ]},



        {itemType: "group",caption: "Sum Insured",colCount:2,items: [
    { dataField: "sumInsuredTotal", editorType:"dxNumberBox", editorOptions: {format: '#,##0'
         } },
    { dataField: "sumInsuredTotalUsd", label: { template: labelTpl("SumInsuredTotalUsd - Total Sum Insured (USD)") } },
    { dataField: "exchangeRate", label: { template: labelTpl("ExchangeRate - Exchange rate") } }
    ]},
    {itemType: "group",caption: "Premium",colCount:2,items: [
                 { dataField: "deductibleId", label: { template: labelTpl("Deductible - Deductible") } },
    { dataField: "premiumRate", label: { template: labelTpl("PremiumRate - Premium rate") } },
    { dataField: "premiumCfe", label: { template: labelTpl("PremiumCfe - CFE premium") } },
    { dataField: "premiumVoluntary", label: { template: labelTpl("PremiumVoluntary - Voluntary premium") } },
    { dataField: "premiumTotalBeforeVat", label: { template: labelTpl("PremiumTotalBeforeVat - Tổng premium trước VAT") } },
    { dataField: "premiumTotal", editorType:"dxNumberBox", editorOptions: {format: '#,##0'

         } }
    ]},
    {itemType: "group",caption: "VAT",colCount:2,items: [
    {dataField: "vat",editorType: "dxTextBox",visible:true},
    {dataField: "vatValue",editorType: "dxTextBox",visible:true}
    ]},

     {
         itemType: "group",
         caption: "Due",
         colCount: 2,
         items: [
             { dataField: "totalDue", editorType:"dxNumberBox", editorOptions: {format: '#,##0'


         } },
             ]
     },
    {itemType: "group",caption: "Installment",colCount:2,items: [
    {dataField: "installmentId",editorType: "dxTextBox",visible:true} //dxDatagrid
    ]},
    {itemType: "group",caption: "Terms & Conditions",colCount:2,items: [
    {dataField: "termsConditionsText",editorType: "dxTextBox",visible:true}
    ]},
    {itemType: "group",caption: "Clause",colCount:2,items: [
    {dataField: "clauseId",editorType: "dxTextBox",visible:true}
    ]},





    {
     itemType: "group",
     caption: "Risk",
     colCount: 3,
     items: [
         { dataField: "riskGrade", label: { template: labelTpl("RiskGrade - Risk grade") } },
             { dataField: "topRiskPd", label: {text: "Top Risk (After Co)", template: labelTpl("TopRiskPd - Top risk PD") } },
             { dataField: "topRiskPdQuotation", label: { template: labelTpl("TopRiskPdQuotation - Top risk PD quotation") } }

     ]
    },
    
     ]
    });
    viewFieldAdjust("@QTViewIdHelper.Form(dept, quotationId)");
</script>
<div id="@QTViewIdHelper.Form(dept, quotationId)"></div>