@page
@model FollowUpDocumentModel
@{
}
<script>
    $(function () {
        const gridParams = {
            isAllowRowMenu: false,
            gridEditorOptions: {
                editing: { allowUpdating: false, allowAdding: false, allowDeleting: false },
                filterRow: {
                    visible: true,
                    applyFilter: "auto"
                },
                headerFilter: {
                    visible: true,
                    allowSearch: true
                }
            }
        };
        const params = new URLSearchParams();
        params.set("refField", "workflowStatus");
        params.set("refKey", "Follow up");
        const gridOption = new PolicyIssuanceGridOption("@FollowUpDocumentModel.ModelName", "User", gridParams);
        gridOption.refKey = "Follow up";
        gridOption.refField = "workflowStatus";
        new MGrid(null, $("#dataGrid_@FollowUpDocumentModel.PageName"), gridOption, null);
    });
</script>
<div id="dataGrid_@FollowUpDocumentModel.PageName"></div>
